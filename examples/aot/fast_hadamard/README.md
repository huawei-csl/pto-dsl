Usage:

```bash
bash ./compile.sh  # generate PTO/CPP and build both auto/manual sync libs
python ./run_hadamard.py  # test auto-sync lib
python ./run_hadamard.py --lib ./hadamard_manual_sync_lib.so  # test manual-sync lib
python ./run_hadamard.py --test-both  # test both libs
```
